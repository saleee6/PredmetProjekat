apiVersion: v1
kind: Service
metadata:
  name: {{ template "usermicroservice.fullname" . }}
  labels:
    app: {{ template "usermicroservice.name" . }}
    chart: {{ template "usermicroservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "usermicroservice.name" . }}
    release: {{ .Release.Name }}
