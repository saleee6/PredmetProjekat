apiVersion: v1
kind: Service
metadata:
  name: {{ template "airlinemicroservice.fullname" . }}
  labels:
    app: {{ template "airlinemicroservice.name" . }}
    chart: {{ template "airlinemicroservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "airlinemicroservice.name" . }}
    release: {{ .Release.Name }}
