apiVersion: v1
kind: Service
metadata:
  name: {{ template "rentacarmicroservice.fullname" . }}
  labels:
    app: {{ template "rentacarmicroservice.name" . }}
    chart: {{ template "rentacarmicroservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "rentacarmicroservice.name" . }}
    release: {{ .Release.Name }}
